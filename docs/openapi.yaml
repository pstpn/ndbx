openapi: 3.0.0
info:
  title: ndbx-service
  version: 0.0.0
tags: []
paths:
  /auth/login:
    post:
      operationId: Api_login
      summary: authenticate user
      parameters:
        - $ref: '#/components/parameters/CookieRequest'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
  /auth/logout:
    post:
      operationId: Api_logout
      summary: logout user
      parameters:
        - $ref: '#/components/parameters/CookieRequest'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '401':
          description: Access is unauthorized.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /events:
    post:
      operationId: Api_createEvent
      summary: create a new event
      parameters:
        - $ref: '#/components/parameters/CookieRequest'
      responses:
        '201':
          description: The request has succeeded and a new resource has been created as a result.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                required:
                  - id
        '401':
          description: Access is unauthorized.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEventRequest'
    get:
      operationId: Api_getEvents
      summary: get events with filtering and pagination
      parameters:
        - $ref: '#/components/parameters/GetEventsRequest.cookie'
        - $ref: '#/components/parameters/GetEventsRequest.id'
        - $ref: '#/components/parameters/GetEventsRequest.title'
        - $ref: '#/components/parameters/GetEventsRequest.category'
        - $ref: '#/components/parameters/GetEventsRequest.price_from'
        - $ref: '#/components/parameters/GetEventsRequest.price_to'
        - $ref: '#/components/parameters/GetEventsRequest.address'
        - $ref: '#/components/parameters/GetEventsRequest.city'
        - $ref: '#/components/parameters/GetEventsRequest.date_from'
        - $ref: '#/components/parameters/GetEventsRequest.date_to'
        - $ref: '#/components/parameters/GetEventsRequest.user_id'
        - $ref: '#/components/parameters/GetEventsRequest.user'
        - $ref: '#/components/parameters/GetEventsRequest.include'
        - $ref: '#/components/parameters/GetEventsRequest.limit'
        - $ref: '#/components/parameters/GetEventsRequest.offset'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetEventsResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /events/{event_id}/reviews:
    post:
      operationId: Api_createEventReview
      summary: create a review for an event
      parameters:
        - $ref: '#/components/parameters/CreateEventReviewParams.event_id'
        - $ref: '#/components/parameters/CreateEventReviewParams.cookie'
      responses:
        '201':
          description: The request has succeeded and a new resource has been created as a result.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                required:
                  - id
        '401':
          description: Access is unauthorized.
        '409':
          description: The request conflicts with the current state of the server.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEventReviewConflictResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateReviewRequest'
    get:
      operationId: Api_getEventReviews
      summary: get reviews for an event
      parameters:
        - $ref: '#/components/parameters/GetEventReviewsParams.event_id'
        - $ref: '#/components/parameters/GetEventReviewsParams.cookie'
        - $ref: '#/components/parameters/GetEventReviewsParams.limit'
        - $ref: '#/components/parameters/GetEventReviewsParams.offset'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReviewsResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /events/{event_id}/reviews/{review_id}:
    patch:
      operationId: Api_updateEventReview
      summary: update a review for an event
      parameters:
        - $ref: '#/components/parameters/UpdateEventReviewParams.event_id'
        - $ref: '#/components/parameters/UpdateEventReviewParams.review_id'
        - $ref: '#/components/parameters/UpdateEventReviewParams.cookie'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '401':
          description: Access is unauthorized.
        '404':
          description: The server cannot find the requested resource.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateEventReviewNotFoundResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateReviewRequest'
  /events/{id}:
    get:
      operationId: Api_getEvent
      summary: get event by id
      parameters:
        - $ref: '#/components/parameters/GetEventRequest.id'
        - $ref: '#/components/parameters/GetEventRequest.cookie'
        - $ref: '#/components/parameters/GetEventRequest.include'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventData'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: Api_patchEvent
      summary: patch event by id
      parameters:
        - $ref: '#/components/parameters/PatchEventParams.id'
        - $ref: '#/components/parameters/PatchEventParams.cookie'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '401':
          description: Access is unauthorized.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchEventRequest'
  /events/{id}/dislike:
    post:
      operationId: Api_dislikeEvent
      summary: dislike event by id
      parameters:
        - $ref: '#/components/parameters/EventReactionParams.id'
        - $ref: '#/components/parameters/EventReactionParams.cookie'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '401':
          description: Access is unauthorized.
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /events/{id}/like:
    post:
      operationId: Api_likeEvent
      summary: like event by id
      parameters:
        - $ref: '#/components/parameters/EventReactionParams.id'
        - $ref: '#/components/parameters/EventReactionParams.cookie'
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '401':
          description: Access is unauthorized.
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /health:
    get:
      operationId: Api_health
      summary: healthcheck with session
      parameters:
        - $ref: '#/components/parameters/HealthRequest'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /session:
    post:
      operationId: Api_session
      summary: create or extend user's session
      parameters:
        - $ref: '#/components/parameters/SessionRequest'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        '201':
          description: The request has succeeded and a new resource has been created as a result.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users:
    post:
      operationId: Api_register
      summary: register a new user
      parameters:
        - $ref: '#/components/parameters/CookieRequest'
      responses:
        '201':
          description: The request has succeeded and a new resource has been created as a result.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegisterRequest'
    get:
      operationId: Api_getUsers
      summary: get users
      parameters:
        - $ref: '#/components/parameters/GetUsersRequest.cookie'
        - $ref: '#/components/parameters/GetUsersRequest.id'
        - $ref: '#/components/parameters/GetUsersRequest.name'
        - $ref: '#/components/parameters/GetUsersRequest.limit'
        - $ref: '#/components/parameters/GetUsersRequest.offset'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUsersResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users/{id}:
    get:
      operationId: Api_getUser
      summary: get user by id
      parameters:
        - $ref: '#/components/parameters/GetUserRequest.id'
        - $ref: '#/components/parameters/GetUserRequest.cookie'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserData'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users/{id}/events:
    get:
      operationId: Api_getUserEvents
      summary: get user events
      parameters:
        - $ref: '#/components/parameters/GetUserEventsRequest.id'
        - $ref: '#/components/parameters/GetUserEventsRequest.cookie'
        - $ref: '#/components/parameters/GetUserEventsRequest.title'
        - $ref: '#/components/parameters/GetUserEventsRequest.category'
        - $ref: '#/components/parameters/GetUserEventsRequest.price_from'
        - $ref: '#/components/parameters/GetUserEventsRequest.price_to'
        - $ref: '#/components/parameters/GetUserEventsRequest.address'
        - $ref: '#/components/parameters/GetUserEventsRequest.city'
        - $ref: '#/components/parameters/GetUserEventsRequest.date_from'
        - $ref: '#/components/parameters/GetUserEventsRequest.date_to'
        - $ref: '#/components/parameters/GetUserEventsRequest.include'
        - $ref: '#/components/parameters/GetUserEventsRequest.limit'
        - $ref: '#/components/parameters/GetUserEventsRequest.offset'
      responses:
        '200':
          description: The request has succeeded.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetEventsResponse'
        default:
          description: An unexpected error response.
          headers:
            Set-Cookie:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  parameters:
    CookieRequest:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    CreateEventReviewParams.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    CreateEventReviewParams.event_id:
      name: event_id
      in: path
      required: true
      schema:
        type: string
    EventReactionParams.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    EventReactionParams.id:
      name: id
      in: path
      required: true
      schema:
        type: string
    GetEventRequest.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetEventRequest.id:
      name: id
      in: path
      required: true
      schema:
        type: string
    GetEventRequest.include:
      name: include
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventReviewsParams.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetEventReviewsParams.event_id:
      name: event_id
      in: path
      required: true
      schema:
        type: string
    GetEventReviewsParams.limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventReviewsParams.offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventsRequest.address:
      name: address
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.category:
      name: category
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/EventCategory'
      explode: false
    GetEventsRequest.city:
      name: city
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetEventsRequest.date_from:
      name: date_from
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.date_to:
      name: date_to
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.id:
      name: id
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.include:
      name: include
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventsRequest.offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventsRequest.price_from:
      name: price_from
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventsRequest.price_to:
      name: price_to
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetEventsRequest.title:
      name: title
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.user:
      name: user
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetEventsRequest.user_id:
      name: user_id
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.address:
      name: address
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.category:
      name: category
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/EventCategory'
      explode: false
    GetUserEventsRequest.city:
      name: city
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetUserEventsRequest.date_from:
      name: date_from
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.date_to:
      name: date_to
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.id:
      name: id
      in: path
      required: true
      schema:
        type: string
    GetUserEventsRequest.include:
      name: include
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserEventsRequest.limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetUserEventsRequest.offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetUserEventsRequest.price_from:
      name: price_from
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetUserEventsRequest.price_to:
      name: price_to
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetUserEventsRequest.title:
      name: title
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUserRequest.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetUserRequest.id:
      name: id
      in: path
      required: true
      schema:
        type: string
    GetUsersRequest.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    GetUsersRequest.id:
      name: id
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUsersRequest.limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    GetUsersRequest.name:
      name: name
      in: query
      required: false
      schema:
        type: string
      explode: false
    GetUsersRequest.offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        format: int64
      explode: false
    HealthRequest:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    PatchEventParams.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    PatchEventParams.id:
      name: id
      in: path
      required: true
      schema:
        type: string
    SessionRequest:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    UpdateEventReviewParams.cookie:
      name: Cookie
      in: header
      required: false
      schema:
        type: string
    UpdateEventReviewParams.event_id:
      name: event_id
      in: path
      required: true
      schema:
        type: string
    UpdateEventReviewParams.review_id:
      name: review_id
      in: path
      required: true
      schema:
        type: string
  schemas:
    CreateEventRequest:
      type: object
      required:
        - title
        - address
        - started_at
        - finished_at
      properties:
        title:
          type: string
        address:
          type: string
        description:
          type: string
        started_at:
          type: string
        finished_at:
          type: string
    CreateEventReviewConflictResponse:
      type: object
      properties:
        message:
          type: string
    CreateReviewRequest:
      type: object
      required:
        - comment
        - rating
      properties:
        comment:
          type: string
        rating:
          type: integer
          format: int32
    ErrorResponse:
      type: object
      properties:
        message:
          type: string
    EventCategory:
      type: string
      enum:
        - meetup
        - concert
        - exhibition
        - party
        - other
    EventData:
      type: object
      required:
        - id
        - title
        - location
        - started_at
        - finished_at
        - created_at
        - created_by
      properties:
        id:
          type: string
        title:
          type: string
        category:
          $ref: '#/components/schemas/EventCategory'
        price:
          type: integer
          format: int64
        description:
          type: string
        location:
          $ref: '#/components/schemas/LocationInfo'
        started_at:
          type: string
        finished_at:
          type: string
        created_at:
          type: string
        created_by:
          type: string
        reactions:
          $ref: '#/components/schemas/EventReactions'
        reviews:
          $ref: '#/components/schemas/EventReviews'
    EventReactions:
      type: object
      required:
        - likes
        - dislikes
      properties:
        likes:
          type: integer
          format: int64
        dislikes:
          type: integer
          format: int64
    EventReviews:
      type: object
      required:
        - count
        - rating
      properties:
        count:
          type: integer
          format: int64
        rating:
          type: number
          format: double
    GetEventsResponse:
      type: object
      required:
        - events
        - count
      properties:
        events:
          type: array
          items:
            $ref: '#/components/schemas/EventData'
        count:
          type: integer
          format: int64
    GetReviewsResponse:
      type: object
      required:
        - reviews
        - count
      properties:
        reviews:
          type: array
          items:
            $ref: '#/components/schemas/ReviewData'
        count:
          type: integer
          format: int64
    GetUsersResponse:
      type: object
      required:
        - users
        - count
      properties:
        users:
          type: array
          items:
            $ref: '#/components/schemas/UserData'
        count:
          type: integer
          format: int64
    HealthResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
    LocationInfo:
      type: object
      required:
        - address
      properties:
        address:
          type: string
        city:
          type: string
    LoginRequest:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
        password:
          type: string
    PatchEventRequest:
      type: object
      properties:
        category:
          $ref: '#/components/schemas/EventCategory'
        city:
          type: string
        price:
          type: integer
          format: int64
    ReviewData:
      type: object
      required:
        - id
        - event_id
        - comment
        - created_at
        - created_by
        - rating
        - updated_at
      properties:
        id:
          type: string
        event_id:
          type: string
        comment:
          type: string
        created_at:
          type: string
        created_by:
          type: string
        rating:
          type: integer
          format: int32
        updated_at:
          type: string
    UpdateEventReviewNotFoundResponse:
      type: object
      properties:
        message:
          type: string
    UpdateReviewRequest:
      type: object
      properties:
        comment:
          type: string
        rating:
          type: integer
          format: int32
    UserData:
      type: object
      required:
        - id
        - full_name
        - username
      properties:
        id:
          type: string
        full_name:
          type: string
        username:
          type: string
    UserRegisterRequest:
      type: object
      required:
        - full_name
        - username
        - password
      properties:
        full_name:
          type: string
        username:
          type: string
        password:
          type: string
servers:
  - url: http://localhost:8080
    description: localhost
    variables: {}
